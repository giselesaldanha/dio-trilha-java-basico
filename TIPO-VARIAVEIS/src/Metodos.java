public class Metodos {
  // Métodos são usados para ações
  // Devem ser nomeados como verbo e seguir o padrão camelCase - nomeDoMetodo (parâmetros) {}
  // Caso o método não retorne nenhum valor ele deve ser representaod pela palavra chave void
  // Um método pode ser visível para toda aplicação, somente em mesmo pacotes, através de herança ou somente à nivel a própria classe.

  boolean ligada = false;
  int canal = 1;
  int volume = 25;

  public static void main(String[] args) {
  //SMART TV
  //Ela tenha as características: ligada (boolean), canal (int) e volume (int);
  //Nossa TV poderá ligar e desligar e assim mudar o estado ligada;
  //Nossa TV aumentará e diminuirá o volume sempre em +1 ou -1;
  //Nossa TV poderá mudar de canal de 1 em 1 ou definindo o número correspondente

  


  }
  
}
